"use client";

import React from 'react';
import { InstagramIcon, WhatsAppIcon, ArrowLeftIcon} from '../ui/Icons';

export default function DetailContent({umkm, onBack}){
    return (
        <div className= {`fixed inset-0  ${animationClass}`}>
            <h1 className='text-2xl font-bold mb-4 text-sky-400'>Selamat datang di kampung bandeng tambakrejo kota semarang</h1>
        </div>
    )
}